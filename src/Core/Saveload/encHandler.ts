import { Encoder, Decoder } from "encoder";
import { Checksum } from "codeChecksum";
import { BinaryReader, BinaryWriter, File, SyncRequest, base64Encode, base64Decode } from "w3ts";

// This takes the code from Save Load encoder the further encode it into TH's BinarySystem
export class SaveEncoder {
	constructor(data: string[]) {}
}
